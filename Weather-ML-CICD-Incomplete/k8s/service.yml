apiVersion: v1
kind: Service
metadata:
  name: weather-app-service
spec:
  type: LoadBalancer
  selector:
    app: weather-ml-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000









